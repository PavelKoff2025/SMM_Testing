# SMM_testing — система автоматизированного тестирования студентов

> Итоговый проект курса **«Вайбкодинг. Профессия»** (Claude Code).
> Автор: Павел Кофф (pavelkoff), НИТУ МИСИС, курс «Взаимодействие с социальными медиа».

## Назначение

Веб-ресурс для автоматизации прохождения тестов студентами после каждой темы (лекции). Студент регистрируется, проходит тест из 10 вопросов, получает зачёт/незачёт. Преподаватель видит сводную таблицу прогресса и итоговый допуск к финальному зачёту по курсу.

## Цели и задачи

- **Для студента:** удобная веб-форма прохождения теста по конкретной лекции с мгновенным результатом.
- **Для преподавателя:** автоматический учёт регистраций, результатов, ошибок по вопросам и итоговый допуск к зачету — без ручной проверки.

## Критерий зачёта

- **Зачёт** — **7 и более** правильных ответов из 10 (незачёт — 6 и менее).
- Порог (7) и количество вопросов (10) вынесены в конфиг (`config.py`) — можно менять без правки логики.

## Структура вопросов (10 шт. на тест)

| Сложность | Количество | Назначение |
|---|---|---|
| Лёгкие | **4** | Базовое понимание темы, фактология |
| Средние | **4** | Применение знаний, анализ кейсов |
| На смекалку и логику | **2** | Нестандартное применение, рассуждение |

- Структура (4 / 4 / 2) фиксируется в конфиге и валидируется при загрузке JSON-файла теста.
- Формат JSON-вопроса включает поле `difficulty: easy | medium | logic`.
- *Опционально:* в кабинете преподавателя можно видеть результаты по уровням сложности (где чаще ошибаются).

## Курс и расписание

- На курсе **9 лекций** → **9 тестов** (по одному на лекцию).
- Преподаватель читает лекции по расписанию. После того как лекция прочитана всем студентам курса, **открывается доступ** к тесту по этой лекции.
- **Жизненный цикл теста**:
  - `draft` — тест создан (из PDF или JSON), скрыт от студентов;
  - `scheduled` — задано время открытия, доступ откроется автоматически;
  - `open` — тест доступен студентам;
  - `closed` — приём ответов окончен.

## Критерий допуска к финальному зачёту

Студент **«допущен»** к финальному зачёту по курсу при выполнении **обоих** условий:
1. **Прошёл все 9 тестов** (по каждой лекции).
2. **Набрал минимум 81 правильный ответ из 90** суммарно по всем тестам (≈ 90 %).

Иначе — **«не допущен»**. Порог (81 из 90) вынесен в конфиг.

## Сценарий работы

### Преподаватель — создание теста из презентации
1. Преподаватель открывает **кабинет преподавателя** (защищённый доступ).
2. Загружает **презентацию лекции в формате PDF**.
3. Система извлекает текст из PDF и **генерирует тест через внешнюю LLM (GPT-4o-mini, OpenAI SDK)**: 10 вопросов в пропорции **4 лёгких / 4 средних / 2 на смекалку и логику**, с правильными ответами.
   - **Гибридный режим:** можно загрузить PDF для автогенерации либо загрузить готовый JSON-файл теста.
4. Тест сохраняется в статусе `draft`, преподаватель видит предпросмотр вопросов.
5. Преподаватель проверяет и **публикует** тест, выбрав способ открытия:
   - **вручную кнопкой** («Открыть доступ») → `open`;
   - **по расписанию** (дата/время) → `scheduled`, доступ откроется автоматически.

### Студент
1. Открывает сайт → видит список тестов по лекциям (доступны только `open` или тесты с наступившим временем `scheduled`).
2. Выбирает тест → регистрируется: **имя, фамилия, группа, корпоративная почта @misis.ru**.
3. Проходит тест (10 вопросов), отправляет ответы.
4. Система проверяет ответы и выставляет оценку (зачёт при **7 из 10**, иначе незачёт).
5. Студент видит результат и количество правильных ответов.
   - **Копия результата отправляется e-mail'ом от имени преподавателя** на корпоративную почту студента (@misis.ru) — см. раздел «Email-уведомления».

### Преподаватель — мониторинг
1. В **кабинете** видит **сводную таблицу** по всем попыткам:
   - студент (имя, фамилия, группа);
   - какой тест прошёл, оценка (зачёт / незачёт);
   - **на какие вопросы ответил неверно** (номер, сложность, правильный ответ).
2. Видит **итоговую таблицу допуска** по каждому студенту:
   - прогресс по всем 9 тестам (пройдено / зачтено / суммарно правильных);
   - статус **«допущен / не допущен»** к финальному зачёту (все 9 + 81 из 90).

## Email-уведомления

- После прохождения теста результат отправляется **от имени преподавателя** на **корпоративную почту студента (@misis.ru)**.
- Письмо содержит: название теста (лекция), количество правильных ответов из 10, оценку (зачёт / незачёт), перечень вопросов, на которые студент ответил неверно, с правильными ответами.
- Отправка через **Яндекс SMTP** (`smtp.yandex.ru`, порт 465 SSL / 587 STARTTLS) с ящика преподавателя **pkarikov@yandex.ru** (требуется пароль приложения Яндекс). Настройки в `.env`: `SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASSWORD`, `SMTP_FROM_NAME`.
- **Плановый переход:** корпоративный ящик `karikov.pv@misis.ru` откроют в **феврале 2027** (после переподписания договора с МИСИС). До этого отправка идёт с Яндекса; адрес отправителя вынесен в конфиг — переключение займёт одну переменную в `.env`.
- При регистрации студент указывает почту `@misis.ru`; система **проверяет домен** (только корпоративные адреса).
- **Отказоустойчивость:** отправка включается флагом `SMTP_ENABLED=1` (по умолчанию `0` — письмо не отправляется, результат виден в кабинете). При недоступности/ошибке SMTP результат всё равно показывается студенту — email-канал не критичный, ошибка логируется. Реализация — `app/services/emailer.py`, вызов из `submit_test` в потоке (`asyncio.to_thread`, не блокирует event loop).

## Стек технологий

| Слой | Технология | Обоснование |
|---|---|---|
| Backend | **Python + FastAPI** | Современный фреймворк, автодокументация (`/docs`), асинхронность, подходит для итогового проекта |
| База данных | **SQLite + SQLAlchemy ORM** | Файл без отдельного сервера, портативность, ORM упрощает запросы преподавателя |
| Фронтенд | **Jinja2 + HTML/CSS** (server-rendered) | Один проект без сборки SPA; результат виден сразу |
| Генерация тестов | **OpenAI SDK + GPT-4o-mini** | Внешняя LLM создаёт 10 вопросов 4/4/2 из текста презентации; дешёвая и быстрая модель |
| Парсинг PDF | **pdfplumber** | Извлечение текста слайдов из загруженной презентации |
| Вопросы тестов | **JSON-файлы по лекциям** | Преподаватель правит вопросы в тексте, без админки БД |
| Расписание доступа | **APScheduler** | Автоматическое открытие тестов в заданное время (`scheduled` → `open`) |
| Email-отправка | **smtplib + email.mime** (SMTP) | Доставка результатов студентам на @misis.ru от имени преподавателя |
| Запуск (локально) | **Uvicorn** | Стандартный ASGI-сервер для FastAPI |

> Альтернативы рассматривались: Flask (проще, но менее «показный» для диплома), PostgreSQL (мощнее, но избыточен для одной группы), React-фронт (отдельная сборка, оверхед для теста из 10 вопросов). Для генерации тестов рассматривалась Anthropic Claude API, но выбран GPT-4o-mini как сторонняя LLM по требованию задачи.

## Структура проекта (планируемая)

```
SMM_testing/
├── README.md                  # этот файл
├── requirements.txt           # зависимости Python
├── .env.example               # шаблон переменных окружения (API-ключ OpenAI и др.)
├── config.py                  # настройки (порог зачёта, 4/4/2, допуск 81/90, доступ преподавателя)
├── app/
│   ├── main.py                # точка входа FastAPI
│   ├── database.py            # подключение SQLite, сессии SQLAlchemy
│   ├── models.py              # модели: Student, Test, Question, Attempt, Answer
│   ├── auth.py                # защита кабинета преподавателя (пароль / токен)
│   ├── routers/
│   │   ├── student.py         # роуты студента: список тестов, регистрация, прохождение
│   │   └── teacher.py         # роуты преподавателя: загрузка PDF, публикация, аналитика, допуск
│   ├── services/
│   │   ├── pdf_parser.py      # извлечение текста из PDF (pdfplumber)
│   │   ├── ai_generation.py   # генерация теста через OpenAI GPT-4o-mini
│   │   ├── questions.py       # загрузка/валидация JSON-вопросов (4/4/2)
│   │   ├── grading.py         # проверка ответов и выставление оценки (7 из 10)
│   │   ├── scheduler.py       # APScheduler: открытие тестов по расписанию
│   │   ├── emailer.py         # отправка результатов студенту на @misis.ru (SMTP)
│   │   └── admission.py       # логика итогового допуска к зачёту (все 9 + 81/90)
│   └── templates/             # Jinja2-шаблоны (список тестов, форма, результат, кабинет)
├── uploads/                   # загруженные PDF-презентации
├── tests_data/                # JSON-файлы с вопросами по лекциям
│   └── lecture_01.json
└── data/
    └── smm_testing.db         # файл SQLite (создаётся при первом запуске)
```

## Модели данных (планируемые)

- **Student** — `id`, `first_name`, `last_name`, `group`, `email` (@misis.ru), `created_at`.
- **Test** — `id`, `lecture_title`, `status` (draft/scheduled/open/closed), `scheduled_at`, `source` (pdf/json), `pdf_path`, `pass_threshold`, `created_at`.
- **Question** — `id`, `test_id`, `number`, `difficulty` (easy/medium/logic), `text`, `options`, `correct_answer`.
- **Attempt** — `id`, `student_id`, `test_id`, `score`, `passed` (bool), `started_at`, `finished_at`.
- **Answer** — `id`, `attempt_id`, `question_number`, `difficulty`, `student_answer`, `is_correct`.

## Запуск локально

```bash
cd SMM_testing
python3 -m venv venv
source venv/bin/activate            # Windows: venv\Scripts\activate
pip install -r requirements.txt
cp .env.example .env                # заполнить OpenAI/SMTP/пароль преподавателя
uvicorn app.main:app --reload       # http://127.0.0.1:8000  (документация: /docs)
```

## Демо-деплой на Render (для защиты проекта)

Живое демо для демонстрации комиссии. Боевой продакшен — отдельная задача,
не раньше февраля 2027. Конфиг — `render.yaml` (Blueprint) в корне репо.

### Что даёт демо-режим (переменные окружения в `render.yaml`)

- `DEMO_AUTOSEED=1` — при старте на пустой БД автоматически заливается демо-тест
  лекции 1 и два студента с попытками (Петров 8/10 и 5/10 → лучшая 8; Смирнова 10/10).
  Нужен, потому что диск на Render free эфемерный: при каждом перезапуске/деплое
  БД SQLite обнуляется, и демо пересоздаётся заново.
- `AI_MOCK=1` — генерация вопросов из PDF работает в мок-режиме (демо-вопросы,
  без вызова OpenAI). Ключ `OPENAI_API_KEY` не нужен.
- `APP_ENV=dev` — старт не падает на дефолтных секретах; HTTPS у Render есть
  из коробки, сессия работает.
- `SECRET_KEY` — генерируется Render автоматически (`generateValue: true`).
- `TEACHER_PASSWORD` — вводится вручную в dashboard Render (`sync: false`),
  в репо не хранится. Для демо комиссии сообщите его отдельно.

### Шаги деплоя

1. На [render.com](https://render.com) залогиньтесь через GitHub.
2. New → **Blueprint**, выберите репо `PavelKoff2025/SMM_Testing`.
3. Render найдёт `render.yaml` и предложит сервис `smm-testing` (free).
4. В разделе Environment сервиса задайте `TEACHER_PASSWORD` (любой, например `demo`).
5. Apply — Render соберёт (`pip install -r requirements.txt`) и запустит
   (`uvicorn app.main:app --host 0.0.0.0 --port $PORT`).
6. Через 1–2 минуты сервис получит URL вида `https://smm-testing.onrender.com`.
7. При каждом пуше в `main` Render пересобирает автоматически (`autoDeploy: true`).

### Что показывать комиссии

- Главная страница — hero, роли, блок автора.
- `https://<url>/` — демо-тест лекции 1 открыт.
- `/student` — зарегистрироваться (`@misis.ru`) и пройти тест; или войти как
  демо-студент (Петров/Смирнова — но они уже с попытками, лучше новый).
- `/teacher` — войти по `TEACHER_PASSWORD`; аналитика и разбор попыток;
  кабинет студента `/student` и инструкции `/student/guide`, `/teacher/guide`.

### Ограничения демо

- Free-тариф: сервис «засыпает» через 15 мин без трафика, холодный старт ~30 сек.
  Перед защитой откройте URL заранее, чтобы разбудить.
- БД обнуляется при перезапуске — демо-данные пересоздаются автоматически,
  но всё, что комиссия введёт вручную, при перезапуске пропадёт (для демо нормально).

## Статус проекта

- [x] Этап 0. Каркас проекта, README, конфиг.
- [x] Этап 1. Модели БД, инициализация SQLite.
- [x] Этап 2. Формат JSON-вопросов (4/4/2) + загрузка/валидация. Демо-тест лекции 1 грузится из `tests_data/lecture_01_demo.json`.
- [x] Этап 3. Роуты студента: регистрация/вход (без пароля, @misis.ru), кабинет, список тестов, прохождение, результат. CSS в стиле МИСИС. Роуты вынесены в `app/routers/student.py`, общие запросы — в `app/services/queries.py`. Сессия — подписанная кука (SessionMiddleware).
- [x] Этап 4. Логика выставления оценки (7 из 10): `score >= pass_threshold` в `submit_test`, страница результата с зачётом/незачётом и посточным разбором ответов. Проверено end-to-end: 10/10 (зачёт) и 3/10 (незачёт), повторная попытка блокируется (одна попытка на тест).
- [x] Этап 5. Кабинет преподавателя: вход по паролю (`TEACHER_PASSWORD`, `hmac.compare_digest`), дашборд со статистикой, загрузка готового JSON-теста и PDF-презентации (гибрид), предпросмотр вопросов, ручная публикация (open/close). Роуты в `app/routers/teacher.py`, защищены сессионной кукой. AI-генерация из PDF — Этап 6.
- [x] Этап 6. AI-генерация теста из PDF: `app/services/pdf_parser.py` (pdfplumber → текст постранично) + `app/services/ai_generation.py` (GPT-4o-mini, `response_format=json_object`; мок-режим `AI_MOCK=1` без ключа) + `test_loader.attach_questions` (валидация через `TestIn` + вставка/замена 10 вопросов в существующий draft-тест). Роут `POST /teacher/test/{id}/generate` с защитами (только PDF, не open/closed, наличие файла). Кнопка «⚡ Сгенерировать вопросы из PDF» в предпросмотре. Конфиг: `AI_MOCK`, `AI_MAX_TEXT_CHARS`.
- [x] Этап 7. Жизненный цикл теста (draft/scheduled/open/closed) + расписание: `app/services/scheduler.py` (AsyncIOScheduler, интервал-задача `open_due_scheduled_tests` — перевод scheduled→open по времени; стартовая «догоняющая» проверка на случай простоя сервера; источник правды — БД, MemoryJobStore). Роуты `POST /teacher/test/{id}/schedule` (ввод по Москве → UTC, валидация 10 вопросов и времени в будущем, запрет из статуса open) и `/unschedule`. Запуск/останов через lifespan FastAPI. Время хранится в UTC (консистентно с `datetime.utcnow()`), ввод/вывод — `config.SCHEDULE_TZ` (Europe/Moscow) через `zoneinfo`. Конфиг: `SCHEDULE_TZ`, `SCHEDULER_INTERVAL_SECONDS`.
- [x] Этап 8. Аналитика: `app/services/analytics.py` — `attempts_summary` (сводная таблица попыток с JOIN Student+Test + число ошибок подзапросом по Answer), `difficulty_error_stats` (агрегат ошибок по сложности прямо по Answer.difficulty — easy/medium/logic: всего/ошибок/%), `attempt_breakdown` (построчный разбор одной попытки). Роуты `GET /teacher/analytics` (stat-row по сложности + таблица всех попыток с ссылкой «Разбор») и `GET /teacher/attempt/{id}` (разбор: выбор студента vs правильный ответ, стиль result.html). Шаблоны `teacher_analytics.html`, `teacher_attempt.html`. Ссылка «Аналитика» в дашборде. Роуты защищены `_require_teacher`.
- [x] Этап 9. Допуск к зачёту: `app/services/admission.py` — `admission_report` (агрегат по студентам: пройдено/зачтено/правильных через LEFT JOIN Attempt + пороги из config: ADMISSION_TESTS_REQUIRED=9 и ADMISSION_CORRECT_REQUIRED=81; сортировка — не допущенные сверху) и `admission_summary`. Роуты `GET /teacher/admission` (сводка + итоговая таблица: ФИО, группа, пройдено/N, зачтено, правильных/90, бейдж «допущен/не допущен») и `GET /teacher/admission.csv` (экспорт CSV, UTF-8 с BOM для Excel, имя по нейминг-формуле `[дата]_admission_report.csv`). Шаблон `teacher_admission.html`. Ссылка «Допуск» в дашборде. Критерий: пройдено все 9 тестов И ≥81 правильных из 90.
- [x] Этап 10а. Контент: 9 тестов сгенерированы из 9 PDF-презентаций лекций через GPT-4o-mini (`AI_MOCK=0`, реальные вызовы API). Структура 4/4/2 проверена у всех 9 тестов (90 вопросов). Контентный QA проведён: исправлены 2 дефекта — test#7 q7 (двусмысленность «мониторинг» vs «социальное прослушивание» — переформулировано с однозначным правильным ответом) и test#10 q9 (дублировал лёгкий q2 — переписан в логический сценарий «выбор Единой перфоманс-кампании»). Демо-тест лекции 1 (JSON) удалён — осталось ровно 9 тестов по 9 лекциям. Все 9 тестов переведены в статус `open` (доступ студентам, без расписания).
- [x] Этап 10б. Харденинг: валидация, обработка ошибок, безопасность. Разведка тремя аудит-проходами выявила критичные места; закрыты:
  - **Секреты:** `APP_ENV` (dev/prod); в prod старт падает, если `TEACHER_PASSWORD`/`SECRET_KEY` не заданы или равны небезопасным дефолтам (`config.ensure_prod_secrets`, вызов из lifespan). Дефолты `"changeme"`/`"dev-secret-change-me"` допустимы только в dev.
  - **Path traversal + OOM:** имя PDF на диске — только `uuid.pdf` (оригинальное имя отброшено); чтение файла порциями с лимитом `MAX_UPLOAD_BYTES` (~26 МБ) — отбивает слишком большие файлы без 500; откат БД удаляет файл-сирота.
  - **Валидация ответов:** `int(raw)` в `submit_test` обёрнут в `try/except` + проверка диапазона `[0, len(options))` — подделанная форма (`q1=abc`) → `-1` (неверно), в БД не мусор.
  - **Race / IntegrityError:** регистрация дубля email и двойной сабмит теста (UNIQUE student+test) обёрнуты в `try/except IntegrityError` → перечитывание и редирект, без 500.
  - **OpenAI:** `timeout=60` на клиенте + обработка пустого `choices` (без `IndexError`).
  - **404/500:** кастомные HTML-страницы (`error.html`) вместо JSON; 500 логируется без раскрытия стека.
  - **Сессия:** `https_only` в prod, `same_site=lax` (частичная CSRF-митигация); регенерация сессии при входе студента/преподавателя (защита от session fixation).
  - **UX:** flash на молчаливых редиректах анонимного студента («чтобы пройти тест — войдите») и при входе преподавателя; мини-JS (`data-disable-on-submit`) блокирует кнопку долгих операций (AI-генерация, загрузка) на время запроса.
  - **`/docs`** закрыт в prod.
  - **Email-уведомления реализованы:** `app/services/emailer.py` (smtplib SSL, plain text) + интеграция в `submit_test` через `asyncio.to_thread` (не блокирует event loop). Ошибка SMTP логируется и **не блокирует** показ результата — студент видит его в кабинете в любом случае. Включается флагом `SMTP_ENABLED` (по умолчанию выкл).
  - **Репетиция:** end-to-end прогон тестовым студентом (зачёт 10/10, повторная попытка → редирект, подделанная форма без 500, чужой результат → 404) пройден без ошибок; тестовые данные удалены.
- [x] Этап 10в. Фичи кабинета студента — таймер и перепрохождение (День 1–3):
  - **Таймер 3 мин на тест** (`config.TEST_TIME_LIMIT_SECONDS=180`): обратный отсчёт от старта попытки, краснеет за `TIMER_DANGER_SECONDS=30` сек до конца. При истечении — попытка закрывается как `timed_out`, следующая открывается через `TEST_COOLDOWN_SECONDS` (24 ч). Кулдаун **только** при таймауте (не при незачёте).
  - **Поток по одному вопросу** (forward-only): вопросы открываются по одному, навигация только вперёд, прыжок назад/вперёд bounce'ит на актуальный. Прогресс-бар `take.html`.
  - **Несколько попыток на тест** (UniqueConstraint убран): в зачёт/допуск идёт **лучшая** попытка. Студент видит в кабинете состояния: «Начать» / «Продолжить (в. N)» / «Доступно с {дата МСК}» (кулдаун) / «Перепройти» + блок «Лучший результат».
  - **Backend (День 1):** `models.py` — `AttemptStatus` (in_progress/completed/timed_out), `Attempt.attempt_number`/`deadline`/`cooldown_until`, per-test `time_limit_seconds`/`cooldown_seconds`; `app/migrate.py` — идемпотентная миграция без Alembic (ADD COLUMN + пересоздание attempts); `queries.py` — `start_attempt`/`save_answer`/`finalize_timed_out`/`finalize_completed`/`get_best_attempt`; `routers/student.py` — переписан под поток с ленивой серверной проверкой таймаута (покрывает закрытие вкладки); `admission.py` — допуск по лучшей попытке.
  - **Фронтенд (День 2):** `student.html` — карточки тестов с состояниями + лучший результат + колонка «Попытка» в таблице; `result.html` — баннер «⏱ Время на попытку истекло» (при timed_out) + блок «Следующая попытка будет доступна {дата МСК}»; `style.css` — прогресс-бар, таймер, `.timer-danger`, баннеры.
  - **Дворник и teacher-views (День 3):** `scheduler.py` — `finalize_stale_attempts` (интервал 30 сек + стартовая проверка: закрывает зависшие `in_progress` с истёкшим deadline — подстраховка к ленивой проверке); аналитика — колонки «Попытка» и «Статус» (завершён/таймаут), разбор попытки показывает № и статус; допуск — колонка «Попыток» + пояснение «в зачёт идёт лучшая попытка» (включая CSV-экспорт). `seed.py` — флаг `--demo-data` (демо-студенты с попытками для демонстрации). `requirements.txt` — pin известных хороших версий (защита от ямы со starlette<1.0).
  - **Проверено end-to-end:** поток 10 вопросов → completed; таймаут → timed_out + кулдаун 24ч + блокировка старта; перепрохождение → лучшая попытка в зачёте; дворник закрывает зависшую in_progress; teacher-views показывают №/статус/лучшую.
- [~] Этап 11. Деплой на VPS. — **отложен до весны 2027** (курс читается весной; до этого проект работает локально).
- [ ] Этап 12. Презентация для защиты — **локальная демонстрация коллегам-преподавателям** на этой машине; деплой для этого не требуется.

## Известные ограничения и принятые решения

Осознанные упрощения для учебного проекта (одна группа студентов МИСИС, закрытая аудитория) — зафиксированы явно, чтобы на защите говорить о них как о решениях, а не как о недосмотрах:

- **Регистрация без пароля и без верификации email.** По README: студент вводит ФИО + группу + `@misis.ru`; домен проверяется. Подтверждение владения ящиком не делается — в одной группе риск имперсонации низок и мотивации нет. При необходимости добавляется код-подтверждение по email (инфраструктура SMTP уже есть).
- **CSRF-митигация через `same_site=lax` + HTTPS**, без полной токен-защиты всех форм. `lax` блокирует cross-site POST (основной вектор CSRF); HTTPS защищает куку в транзите. Полная двойная-submit-cookie защита отложена — для одной группы текущего уровня достаточно.
- **Нет rate-limiting на входе преподавателя.** Закрытая аудитория, пароль не brute-force'ится извне; `hmac.compare_digest` защищает от тайминг-атак. Добавляется in-memory limiter при выходе в открытый интернет.
- **Один преподаватель.** Принадлежность теста не проверяется (некому чужое открыть); пароль хранится в env (не хеш) — допустимо для единственного доверенного пользователя.
- [~] Этап 11. Деплой на VPS. — **отложен до весны 2027** (курс читается весной; до этого проект работает локально).
- [ ] Этап 12. Презентация для защиты — **локальная демонстрация коллегам-преподавателям** на этой машине.

## План реализации (20 авг — 27 авг, досрочно)

> Сборка локально, затем презентация. Дедлайн сдвинут с 3 сентября на **27 августа** (досрочно). Этапы 0–10 выполнены локально; деплой (этап 11) отложен до весны 2027 (курс читается весной), презентация (этап 12) — локальная демонстрация коллегам-преподавателям на этой машине.
> Ритм: ~1–2 ч/день, тяжёлую работу делает Claude Code, преподаватель ревьюит и тестирует.
> Буфер: день 15 имеет резерв; если сборка отстаёт — забираем день 14 (деплой совмещаем с харденингом).

| День | Дата | Этап | Задачи | Результат |
|---|---|---|---|---|
| 1 | 20 авг | 0 | Каркас: venv, requirements, FastAPI hello, config.py, структура папок, git | Локальный пустой проект запускается |
| 2 | 21 авг | 1 | SQLAlchemy-модели (Student/Test/Question/Attempt/Answer), init SQLite, seed | БД создаётся, модели работают |
| 3 | 22 авг | 2 | JSON-схема вопросов 4/4/2, валидация, загрузчик, демо-тест лекции 1 вручную | Один тест грузится из JSON |
| 4 | 23 авг | 3 | Роуты студента: список тестов, регистрация, страница прохождения + CSS | Студент проходит демо-тест |
| 5 | 24 авг | 4 | Grading (7 из 10), страница результата, end-to-end одного теста | Студент видит зачёт/незачёт |
| 6 | 25 авг | 5 | Кабинет преподавателя: авторизация (пароль), дашборд, форма загрузки PDF | Преподаватель входит и грузит PDF |
| 7 | 26 авг | 6 | pdfplumber + OpenAI GPT-4o-mini → JSON 4/4/2, предпросмотр, сохранение draft | Тест генерируется из PDF |
| 8 | 27 авг | 7 | Жизненный цикл: кнопка «Открыть доступ», APScheduler (scheduled→open), гибрид JSON | Тесты публикуются вручную и по расписанию |
| 9 | 28 авг | 8 | Аналитика: сводная таблица попыток с ошибками по сложности | Преподаватель видит ошибки студентов |
| 10 | 29 авг | 9 | Допуск (все 9 + 81/90), итоговая таблица, экспорт CSV | Статус «допущен/не допущен» |
| 11 | 30 авг | 10 | Контент: генерация 9 тестов из 9 PDF, QA, баг-фиксы | Все 9 тестов готовы и протестированы |
| 12 | 31 авг | 10 | Харденинг: валидация, ошибки, безопасность, локальная репетиция | Полный прогон с тестовыми студентами |
| 13 | 1 сен | 11 | ~~Деплой на VPS (Beget)~~ — **отложен до весны 2027** | Локальный режим (текущий) |
| 14 | 2 сен | 12 | Презентация (часть 1): проблема/решение, архитектура, скриншоты, демо | Черновик слайдов |
| 15 | 3 сен | 12 | Презентация (часть 2) + репетиция защиты | Готовая презентация к защите |

> **Поверх плана (25–26 авг):** реализованы фичи кабинета студента по ТЗ автора — таймер 3 мин на тест, поток по одному вопросу, перепрохождение с кулдауном 24ч при таймауте, в зачёт идёт лучшая попытка (Этап 10в, см. «Статус проекта»). Без деплоя — работает локально.

## Открытые вопросы

1. ~~Количество тестов~~ — **решено: 9 (по числу лекций).**
2. ~~Порог зачёта~~ — **решено: 7 из 10.**
3. ~~Критерий допуска~~ — **решён: все 9 тестов + 81 из 90.**
4. ~~Генерация тестов~~ — **решено: GPT-4o-mini (OpenAI SDK), гибрид с JSON.**
5. ~~Доступ студентам~~ — **решено: вручную + по расписанию.**
6. ~~Формат презентаций~~ — **решено: PDF.**
7. ~~Доставка результата студенту~~ — **решено: email от имени преподавателя на @misis.ru.**
8. ~~VPS~~ — **решено: Beget.**
9. ~~API-ключ OpenAI~~ — **решено: ключ есть.**
10. ~~SMTP-ящик преподавателя~~ — **решено: `pkarikov@yandex.ru` через Яндекс SMTP** (пароль приложения). С февраля 2027 → `karikov.pv@misis.ru`.
11. ~~Домен~~ — **пока нет, купим на Beget позже.** До покупки — доступ по IP-адресу VPS или техподдомену Beget.